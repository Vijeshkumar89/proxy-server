from setuptools import setup, find_packages

setup(
    name="proxy-server",
    version="1.0",
    description="HTTP Forward Proxy Server",
    author="Vijesh Kumar Dhakharwal",
    packages=find_packages(),
)
